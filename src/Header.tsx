import React from "react";

const Header = () => {
    return (
        <div>
            <a href="#a">Home</a>
            <a href="#b">CSS</a>
            <a href="#c">JavaScript</a>
        </div>
    );
}

export default Header;